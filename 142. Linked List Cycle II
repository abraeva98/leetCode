var detectCycle = function(head) {
    if (!head) return null;
    let node = head;
    let seen = new Set();
    while (node !== null) {
        if (seen.has(node)) {
            return node;
        }
        seen.add(node);
        node = node.next;
    }
    return null;
};
